Suno media loop (PERSONAL-CHROME mode) — fully complete ONE song this cycle for music.megabyte.space with ALL 3 files (WAV + stems + video). Work in ~/emdash/repositories/music.megabyte.space. This variant drives Brian's NORMAL (unflagged) Chrome — which has NO Cloudflare Turnstile wall — NOT the automation Chrome. Do NOT use scripts/suno-focus-tab.sh (that targets the automation Chrome).

STEP 1 GUARD: run `bash scripts/suno-loop-guard.sh`. Only STOP if it exits !=0 (Chrome not running / no suno tab). Else proceed.

STEP 2 PICK: read data/suno-download-queue.json. If EVERY song status is 'done' -> CronDelete this job + stop (all 109 done). Else pick the FIRST song whose status != 'done', preferring partial:* then blocked then pending. For a 'blocked' track, its stored sunoId is the WRONG-length take: RE-MATCH first — in Suno Library search type the sunoTitle (from data/blocked-rematch-worklist.json), pick the take whose duration matches siteMMSS +/-2s (if two match, play+listen), open it to read the correct sunoId, set the queue entry's sunoId + durMatch:true before downloading. (Most current picks are partial:wav with a correct sunoId — just direct-nav.)

STEP 3 SUNO (personal Chrome): get_screenshot via mcp__desktop-control__computer. Identify Brian's NORMAL Chrome window — it has the regular bookmarks bar (iMacros, Google Trends, Wayback Machine, ...) and NO "Chrome is being controlled by automated test software" banner. If that window is NOT frontmost (e.g. Emdash/Warp is), click its address bar (~250,84) to raise it. Confirm Suno is loaded + logged in as brian404 (Library / a song page renders). If the personal Chrome is not open at all, STOP + report "personal Chrome not open — reopen suno.com in your normal Chrome, logged in as brian404, and keep it frontmost". If the user is actively typing in another app, stand down this fire.

STEP 4 DOWNLOAD every file MISSING from public/media, via Computer Use. First direct-nav: click the address bar (~250,84), type suno.com/song/<sunoId>, Return; wait ~4s; screenshot; verify the bottom player duration matches siteMMSS +/-2s. Coordinates DIFFER from the automation Chrome (narrower window) and the ⋯ Y shifts by description length (~358 for a 1-line style description, ~372 for 2-line) — ALWAYS screenshot to confirm actual positions. Then retrieve each MISSING file:
(a) WAV — skip if public/media/<trackId>.wav already exists (most partials already have it). Else caret/three-dot ⋯ (~670, 358-372) -> Download (~625,518-532) -> WAV Audio -> Download File promptly.
(b) Stems — ⋯ -> Download (hover to open submenu; it opens to the LEFT, items ~x=490) -> Get Stems / MIDI (~490,587-601) -> the Extract modal opens -> click Extract (~613,571) -> wait ~50s for the stem waveforms to finish rendering (screenshot to confirm) -> click the modal's Download (~615,743) -> in the format dialog tick MP3 + WAV + MIDI (checkboxes ~x=256, y ~363/415/467) -> click Download (~477,691) -> close the stems modal (X ~652,195).
(c) Video — ⋯ -> Download -> Video (~490,619-632) -> wait (auto-downloads after generation).
NOTE: on the unflagged personal Chrome there is NO Turnstile — extraction just works. If Chrome shows a "Download multiple files" permission bubble, click Allow (it becomes sticky). Poll `ls -lat ~/Downloads` until every missing file exists. Rename each download to the SITE title (queue.title) if Suno saved it under a different title.

STEP 5 INGEST: export CLOUDFLARE_API_KEY="$(/Users/Apple/.local/bin/get-secret CLOUDFLARE_API_KEY)"; export CLOUDFLARE_EMAIL="blzalewski@gmail.com"; export CLOUDFLARE_ACCOUNT_ID="84fa0d1b16ff8086dd958c468ce7fd59"; then `npm run suno:ingest`. Confirm the song flips to 'done' (needs wav+stems+video ALL present). Verify prod serves all 3 (Range -> 206). git add data/suno-download-queue.json && git commit -m "chore(suno-queue): <title> done [personal chrome]" && git push.

STEP 6 REPORT: song title, the 3 files landed, X/109 complete.

RULES: fully complete ONE song per fire (all 3 files). Never delete/resubmit/trash Suno content. Never buy credits/add-ons. Stay strictly on the Suno tab of the personal Chrome — never touch other tabs/windows. If a step genuinely fails, set that song's status to 'blocked' with a note + stop; the next fire takes the next song.
