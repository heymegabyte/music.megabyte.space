Suno media loop (PERSONAL-CHROME mode) — fully complete ONE song this cycle for music.megabyte.space with ALL 3 files (WAV + stems + video). Work in ~/emdash/repositories/music.megabyte.space. This variant drives Brian's NORMAL (unflagged) Chrome — which has NO Cloudflare Turnstile wall — NOT the automation Chrome. Do NOT use scripts/suno-focus-tab.sh (that targets the automation Chrome).

STEP 1 GUARD: run `bash scripts/suno-loop-guard.sh`. Only STOP if it exits !=0 (Chrome not running / no suno tab). Else proceed.

STEP 2 PICK: read data/suno-download-queue.json. COMPLETION now requires EVERY song to be status:'done' AND midi:true (Brian 2026-09-02, said twice: every song MUST have all 3 stem formats incl MIDI) — or status:'unavailable'. If that holds -> CronDelete this job + stop. Else pick THIS fire's target: (A) if any song has status != 'done' (and != 'unavailable'), pick the FIRST one (prefer partial:* then blocked then pending) and FULL-download all 3 files (WAV + all-3 stems incl MIDI + video). (B) ELSE (every song done/unavailable but some have midi:false or no midi field) do a STEMS-ONLY MIDI BACKFILL on the FIRST such 'done' song (worklist: data/midi-backfill-worklist.json). PROD-FIRST CHECK (proven st-johns-halo + danny-s-strike-suit 2026-09-03 — saved 2 whole Computer Use cycles): BEFORE any Suno interaction, check whether the stems are ALREADY live WITH MIDI — some ORIGINAL-batch downloads already bundled MIDI and only the queue midi flag is stale. Run `unzip -l public/media/stems/<id>.zip | LC_ALL=C grep -ciE '\.mid'`; if >0 AND the local zip is byte-identical to prod (`md5 -q public/media/stems/<id>.zip` == the prod R2 ETag from `curl -s -r 0-0 -D - -o /dev/null https://music.megabyte.space/media/stems/<id>.zip | LC_ALL=C grep -i '^etag:'` — an R2 single-part etag IS the content MD5, no dash), then MIDI is already on prod → just set that entry midi:true (node fs edit), verify all 3 prod 206, commit + push. NO Computer Use, NO re-download, NO re-extract — a stale-flag data-fix is NOT a song download, so it does NOT spend the fire's ONE-song budget (you may clear several stale flags in one fire). ONLY when local MIDI count == 0 (or the local zip differs from prod) is it a GENUINE backfill needing the flow below: its sunoId is already the correct take -> direct-nav suno.com/song/<sunoId> -> ⋯ Download -> Get Stems/MIDI -> Extract (server-cached, waveforms render instantly) -> Download -> tick ALL 3 (MP3+WAV+MIDI) -> Download; the WAV + video already exist, do NOT re-download them; if a WITHOUT-MIDI "<Title> Stems.zip" already sits in ~/Downloads delete it first, then rename the all-3 zip to "<Title> Stems.zip" (use `unzip -l "<zip>" | grep -ciE '\.(mid|midi)( |$)'` to tell them apart — the all-3 has 8); then `npm run suno:ingest` (overwrites the R2 stems zip), set that queue entry midi:true (node fs edit), verify prod media/stems/<id>.zip is 206, commit + push, STEP 6 report — done for this fire. For a full-download target (A) continue below. For a 'blocked' track, its stored sunoId is the WRONG-length take. NULL-SITESEC FIRST-CHECK (proven the-smell-was-so-sinful 2026-09-02): MANY 'blocked' tracks (whole albums — hobbit-passover, flawless-execution, welcome-to-the-multiverse) were blocked only because the queue's siteSec is NULL (build script never captured it), NOT because the sunoId is wrong. BEFORE any Suno search, `ffprobe -v error -show_entries format=duration -of default=nk=1:nw=1 public/audio/<file>.mp3` (site 'file' from src/data.ts) to get ground-truth siteSec; if |ffprobe − stored sunoSec| ≤ 2 the MAPPED sunoId is ALREADY the correct take → skip re-match entirely: set siteSec/siteMMSS/durMatch:true + status 'pending', direct-nav suno.com/song/<sunoId>, verify player ≈ siteMMSS, download all 3. Only when ffprobe is FAR from stored sunoSec do the RE-MATCH search below. DEAD-END REFINEMENT (proven algenib-crown-of-the-sky + let-harmony-begin 2026-09-02): the true dead-end signature is site duration LONGER than EVERY available Suno take (site 5:46/6:49 but all takes ≤5:35 — the extended/composite source was superseded/deleted) → mark 'unavailable'. RE-MATCH first (PROVEN flow, birch-swing-heaven 2026-09-01): click Library (sidebar ~99,364) -> click the Search box (~376,254) -> type the sunoTitle (from data/blocked-rematch-worklist.json). Suno shows the 2 takes with durations on the thumbnails; pick the one matching siteMMSS +/-2s (NOT the wrongSunoSec one). Click that result's TITLE to open its page. Read the correct sunoId RELIABLY via clipboard: click address bar (~250,84) -> cmd+a -> cmd+c -> Bash `pbpaste` + grep the UUID (never OCR the truncated bar). Confirm != wrongSunoId. VERIFY duration: Escape, click the page's big play (~618,394), screenshot -> bottom player total must be siteMMSS +/-2s. IF the sunoTitle search yields NO take matching siteMMSS (worklist sunoTitles can be WRONG/swapped — e.g. mercy-border's was "Border Mercy" but the real distinct clip is titled "Mercy Border"), search the SITE title (queue.title) instead; when found, correct the worklist sunoTitle in the same edit. APOSTROPHE/PUNCTUATION SEARCH GOTCHA (2026-09-02): Suno's search is punctuation-sensitive and inconsistent — "St John's Plate" needed a CURLY apostrophe (U+2019 ’) to return results while "St John's Towers" returned nothing for ANY apostrophe variant but matched a bare distinctive substring "Towers". So if a full-title search returns "No songs found", FALL BACK to a single distinctive word from the title (e.g. "Towers", "Crossfire") — most robust, sidesteps apostrophe/period variants entirely. RE-MATCH DEAD-END (proven saint-johns-plate 2026-09-02): if NO take matches siteMMSS across sunoTitle + site-title + distinctive-substring searches (e.g. site is 3:53 but Suno only has 3:00 + 3:08 takes under that title — the exact source take was renamed/superseded/deleted), do NOT force a wrong-length take and do NOT leave it 'blocked' (the loop would re-pick it every fire and spin forever). First CONFIRM ground truth: `ffprobe -v error -show_entries format=duration -of default=nk=1:nw=1 public/audio/<file>.mp3` (the site 'file' field from src/data.ts) — proves the real site duration. Then set the queue entry status:'unavailable' + durMatch:false + a note explaining the gap (this status sorts LAST via rank ??3, so the loop advances to the next real blocked song instead of looping). Then pick the NEXT song this same fire. MULTI-TAKE DISAMBIGUATION (proven betelgeuse-passover 2026-09-02): album TITLE tracks can have MANY regenerations (6+ takes all sharing the title+lyrics, durations e.g. 3:43/3:55/3:56/4:02/4:03/4:06). When several takes sit near siteMMSS, the ±2s rule STILL uniquely identifies the source IF you use the site's EXACT duration: `ffprobe -v error -show_entries format=duration -of default=nk=1:nw=1 public/<file>` (e.g. 240.92s), then the ONLY qualifying take is the one within ±2s of that exact value (240.92±2 = 238.9–242.9 → a 242s/"4:02"-thumbnail take qualifies, a 243s/"4:03" does NOT). NOTE the thumbnail MM:SS ROUNDS UP but the bottom PLAYER shows the truer value (thumbnail "4:02" played as "4:01" = 240.9s) — trust the player + the exact ffprobe over the thumbnail. To scroll the Library results list use the computer `scroll` action with text like "down:8" (NOT scroll_direction/amount). Then set the queue entry's sunoId + sunoSec + sunoMMSS + durMatch:true (node fs edit) before downloading. NOTE blocked tracks need the WAV too (partials already had it) AND the Suno title != site title -> rename EVERY download from sunoTitle to queue.title (SITE title) before ingest, else norm-match fails. (partial:wav picks already have a correct sunoId — just direct-nav, no WAV needed.)

STEP 3 SUNO (personal Chrome): get_screenshot via mcp__desktop-control__computer. Identify Brian's NORMAL Chrome window — it has the regular bookmarks bar (iMacros, Google Trends, Wayback Machine, ...) and NO "Chrome is being controlled by automated test software" banner. If that window is NOT frontmost (e.g. Emdash/Warp is), click its address bar (~250,84) to raise it. Confirm Suno is loaded + logged in as brian404 (Library / a song page renders). If the personal Chrome is not open at all, STOP + report "personal Chrome not open — reopen suno.com in your normal Chrome, logged in as brian404, and keep it frontmost". If the user is actively typing in another app, stand down this fire.

STEP 4 DOWNLOAD every file MISSING from public/media, via Computer Use. First direct-nav: click the address bar (~250,84), type suno.com/song/<sunoId>, Return; wait ~4s; screenshot; verify the bottom player duration matches siteMMSS +/-2s. Coordinates DIFFER from the automation Chrome (narrower window) and the ⋯ Y shifts by description length (~358 for a 1-line style description, ~372 for 2-line) — ALWAYS screenshot to confirm actual positions. Then retrieve each MISSING file:
(a) WAV — skip if public/media/<trackId>.wav already exists (most partials already have it). Else caret/three-dot ⋯ (~670, 358-372) -> Download (~625,518-532) -> WAV Audio -> Download File promptly. WAV-NULL STALL (proven the-night-knew-my-name 2026-09-02): after heavy session downloading the WAV "Download File" can save a 4-BYTE file whose entire content is the literal text "null" (not a real WAV — `head -c 4` = `null`, `file` says "ASCII text"). This is the SAME stuck client-side download state as the stems STALL FIX below, just manifesting on the WAV. FIX = identical: delete the 4-byte stub, cmd+R RELOAD the page (wait ~4s), reopen ⋯ -> Download -> WAV Audio -> wait for the filename to show -> Download File; the real ~50-60MB WAV lands. ALWAYS byte-check every WAV after download (`stat -f%z` > 1_000_000, or ffprobe duration ≈ siteMMSS) before trusting it — a 4-byte "null" passes a naive existence check but is a failed download.
(b) Stems — ⋯ -> Download (hover to open submenu; it opens to the LEFT, items ~x=490) -> Get Stems / MIDI (~490,587-601) -> the Extract modal opens -> click Extract (~613,571) -> wait ~50s for the stem waveforms to finish rendering (screenshot to confirm) -> click the modal's Download (~615,743) -> in the format dialog tick ALL 3 — MP3 + WAV + MIDI (checkboxes ~x=256, y ~363/415/467) -> click Download (~477,691) -> close the stems modal (X ~652,195). WIDE-WINDOW VARIANT (proven you-almost-had-me 2026-09-02): the personal Chrome sometimes runs WIDE (~1010px, with a right-hand "related songs" sidebar and extra bookmark folders). The main song column stays left so ⋯ is still ~672,358, BUT the Download submenu then opens to the RIGHT (items ~x=700-820: MP3 720,524 / WAV 740,556 / Get Stems 753,587 / Video 727,619), and every modal (WAV / Extract / format picker) is CENTERED: WAV Download-File ~614,499 + close ~674,405; Extract button ~728,571; stems Download ~731,743; format checkboxes ~x=371 (MP3 363 / WAV 415 / MIDI 467); format Download ~592,691; stems-modal close X ~768,195. ALWAYS screenshot to confirm which layout you're in before every click — narrow=submenu-LEFT, wide=submenu-RIGHT+centered-modals. ALWAYS INCLUDE MIDI (checkbox y~467) — Brian directive 2026-09-02 (said twice: "it should select all 3" + "make sure all of the songs have MIDI downloaded"): EVERY song's stems package MUST contain all 3 formats incl MIDI; NEVER skip MIDI. MIDI "may take longer to process" and can slow the zip prepare — if it hangs, apply the STALL FIX below (reload), but NEVER drop MIDI to dodge the stall. STALL FIX (proven almost-heartbreak 2026-09-02): if the stems zip never lands after ~90s (the "Preparing…" spinner reverts to "Download" with no download, no crdownload — a stuck client-side download state after heavy session downloading), RELOAD the page (cmd+R, wait ~4s), then reopen ⋯ -> Download -> Get Stems/MIDI (extraction is CACHED server-side so the waveforms show instantly, no re-Extract) -> Download -> tick WAV -> Download. The zip lands in ~25s after a fresh reload. Do NOT keep re-clicking Download on the stuck modal — reload is the fix, not retry.
(c) Video — ⋯ -> Download -> Video (~490,619-632) -> wait (auto-downloads after generation).
NOTE: on the unflagged personal Chrome there is NO Turnstile — extraction just works. If Chrome shows a "Download multiple files" permission bubble, click Allow (it becomes sticky). Poll `ls -lat ~/Downloads` until every missing file exists. Rename each download to the SITE title (queue.title) if Suno saved it under a different title. HASH-PREFIX NAMING GOTCHA (proven become-the-person-you-needed 2026-09-02): a Suno title that STARTS WITH "#" (e.g. "# Become the Person You Needed") makes the browser save the STEMS zip as a BARE "Stems.zip" (no title at all — grep for `become` misses it; look for a fresh `~/Downloads/Stems.zip`) and the WAV/MP4 with the literal "# " prefix kept ("# <Title>.wav"/".mp4"); rename ALL THREE (`Stems.zip`→"<queue.title> Stems.zip", "# <Title>.<ext>"→"<queue.title>.<ext>") before ingest or norm-match fails. The stem entries INSIDE that zip are also title-less ("(Synth).mp3" etc.) — harmless, ingest only cares about the zip filename.

STEP 5 INGEST: export CLOUDFLARE_API_KEY="$(/Users/Apple/.local/bin/get-secret CLOUDFLARE_API_KEY)"; export CLOUDFLARE_EMAIL="blzalewski@gmail.com"; export CLOUDFLARE_ACCOUNT_ID="84fa0d1b16ff8086dd958c468ce7fd59"; then `npm run suno:ingest`. Confirm the song flips to 'done' (needs wav+stems+video ALL present). VERIFY the stems zip carried MIDI + set the queue entry midi:true: `unzip -l public/media/stems/<id>.zip | grep -ciE '\.(mid|midi)( |$)'` must be >0 (the all-3 zip has 8) — then node-fs-edit the queue entry's `midi:true`. If it's 0, the MIDI checkbox was missed → re-download the stems with all 3 before committing. SHELL-SAFETY (proven st-johns-plate-2 2026-09-03, cost a 10-min timeout): (1) when a stem zip's INTERNAL entry names contain a CURLY apostrophe (’ U+2019, e.g. "St John’s Plate (FX).mid"), a plain `grep -ciE '\.mid$'` returns a FALSE 0 (invalid-UTF-8 lines are skipped) — ALWAYS pipe through `LC_ALL=C grep` for these counts. (2) NEVER use `ls -t <glob> | head` to grab a filename — the interactive shell aliases `ls` to long-format, so `$f` becomes a whole `-rw-r--r-- …` line and the next `unzip -l "$f"` (a filename starting with `-`) prints paged help → a 10-MINUTE HANG. Use `/bin/ls -1`, a bare `<glob>` passed straight to `unzip`/`mv`, or a Node fs script. Verify prod serves all 3 (Range -> 206). git add data/suno-download-queue.json data/midi-backfill-worklist.json && git commit -m "chore(suno-queue): <title> done — all 3 incl MIDI [personal chrome]" && git push.

STEP 6 REPORT: song title, the 3 files landed, X/109 complete.

RULES: fully complete ONE song per fire (all 3 files). Never delete/resubmit/trash Suno content. Never buy credits/add-ons. Stay strictly on the Suno tab of the personal Chrome — never touch other tabs/windows. If a step genuinely fails, set that song's status to 'blocked' with a note + stop; the next fire takes the next song.
